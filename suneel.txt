devops practice

git clone repo-url

ls

cd repo-name

ls

git status

git add .

git commit -m "added"

git config --global user.email "user.email"

git config --global user.name "username"

git commit -m "added"

git push origin main
